version: "2"
sql:
  - engine: "sqlite"
    queries: "queries/"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_empty_slices: true
        emit_json_tags: true
        emit_pointers_for_null_types: true
